<%= form_with model: @log_entry, local: true do |form| %>
  <% if @log_entry.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@log_entry.errors.count, "error") %> prohibited
        this log_entry from being saved:
      </h2>
      <ul>
        <% @log_entry.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :user %><br>
    <%= form.text_field :user %>
  </p>
  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>
  <p>
    <%= form.label :for_date %><br>
    <%= form.text_field :for_date %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
<% end %>